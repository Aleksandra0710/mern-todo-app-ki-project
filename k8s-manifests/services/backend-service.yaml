apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 5000      # порт за Kubernetes мрежа
      targetPort: 8000 # порт од Docker Hub backend имиџ